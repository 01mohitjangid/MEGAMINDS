"""ORM models package.

Importing this package must import every model module so that all tables are
registered on `Base.metadata` — Alembic's autogenerate relies on that. Models
are introduced from Phase 2 onward; this file lists them as they're added.
"""

# from app.models.user import User  # noqa: F401  (Phase 2)
